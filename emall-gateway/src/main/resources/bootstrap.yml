spring:
  application:
    name: emall-gateway
  cloud:
      nacos:
        config:
          server-addr: 47.111.191.111:8848 #配置中心的地址
          file-extension: yml #配置文件结尾的配置
          shared-dataids: emall-nacos.yml #图灵商城公共配置
          refreshable-dataids: emall-nacos.yml
  profiles:
    active: dev

